using System.Windows;

namespace CurrencyConverter
{
    public partial class MainWindow : Window
    {
        /// <summary>
        /// Main window of the application that serves as the primary user interface container.
        /// Handles window-specific initialization and event handling.
        /// </summary>
        public MainWindow()
        {
            /// <summary>
            /// Initializes the XAML components.
            /// </summary>
            InitializeComponent();
        }
    }
}
